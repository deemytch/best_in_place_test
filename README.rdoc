==  setup

    rake db:migrate db:seed
    rails s

== use

Then click to rollout tree at the right side and click any dish

Dish edit view will be loaded with ajax

`:place_holders` are not working, but all other functionality of `best_in_place` - does
